{{#power-select
  options=years
  selected=year
  triggerClass="year-select"
  onchange=(action (mut year))
as |year|
}}
  {{year}}
{{/power-select}}

{{#ember-table as |t|}}
  {{t.head columns=columns}}
  {{t.body rows=model.items}}
{{/ember-table}}
