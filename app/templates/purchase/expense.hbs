{{#page-container}}
  <div class="card">
    <div class="card-body">
      <div class="row">
        <div class="col-md-2">
          {{#power-select
            options=ranges
            selected=range
            triggerClass="range-select"
            onchange=(action (mut range))
          as |range|
          }}
            {{range}}
          {{/power-select}}
        </div>
      </div>
      {{basic-table table}}
    </div>
  </div>
{{/page-container}}
